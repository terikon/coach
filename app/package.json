{
  "name": "coach-app",
  "version": "0.0.1",
  "description": "coach-app",
  "scripts": {
    "server": "cd dist && node server.js",
    "serve": "vue-cli-service serve",
    "build": "vue-cli-service build --mode dev",
    "watch": "vue-cli-service build --mode dev --watch",
    "watchserver": "npm run watch && npm run server"
  },
  "dependencies": {
    "moment": "^2.24.0",
    "node-static": "^0.7.10",
    "socket.io": "^2.0.4",
    "vue": "^2.6.10"
  },
  "devDependencies": {
    "@vue/cli-service": "^3.11.0",
    "vue-template-compiler": "^2.6.10"
  },
  "postcss": {
    "plugins": {
      "autoprefixer": {}
    }
  },
  "browserslist": [
    "> 1%",
    "last 2 versions"
  ]
}
